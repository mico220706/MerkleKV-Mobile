name: merkle_kv_mobile_workspace
description: Workspace configuration for MerkleKV Mobile monorepo
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

# This is a workspace root file for Melos monorepo management
# Individual packages have their own pubspec.yaml files
dev_dependencies:
  flutter_lints: ^3.0.0
  melos: ^4.0.0
